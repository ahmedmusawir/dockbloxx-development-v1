# Session Log: 2026-05-07

## Project Context
- **Project:** DockBloxx — Headless WooCommerce on Next.js 15
- **Repo path:** `/home/moose/nextjs/DOCKBLOXX/DEV-STAGING/dockbloxx-development-v1`
- **Tool:** Claude Code (Opus 4.7, 1M context)
- **Goal:** Full discovery / context restore. Tony explicitly asked Claude to read CLAUDE.md, README.md, and every doc listed there + every file in `/docs`, and to stand up session + RECOVERY files before the next task.

## Starting State
- **Branch:** main
- **Last commit:** `9a5fb23 10feb2026 - Dev w Google Cloud Deployment`
- **Last session file:** `session-2026-02-04.md` (~3 months stale)
- **RECOVERY.md:** did not exist before this session
- **Last working feature (per Feb 4 session log):** Full GHL attribution + coupon auto-fill + build fix; pending: staging deploy of WP plugin and Coach's attribution script
- **Known issues (per Feb 4 session log):** GHL "Create Or Update Opportunity" workflow was failing with stageId mismatch — root cause: GHL pipeline stage UUIDs changed; Coach was advised to clear the custom-value override on the stage field. Status as of session end: instructions given, not yet verified.
- **Working tree before this session:** `D .windsurf.md`, `D AGENTS.md`, untracked `CLAUDE.md`, untracked `WINDSURF.md`. (CLAUDE.md/WINDSURF.md are present but not yet committed; .windsurf.md and AGENTS.md were deleted from index.)

## Session Progress

### 12:52 — IN PROGRESS
**Task:** Discovery / context restore (no code changes)
**Status:** Reading per CLAUDE.md Discovery Protocol

**Files read:**
- `CLAUDE.md` (loaded into context by harness)
- `README.md` (2 lines — claims this dir is "production build, no code changes" — contradicts dir name `DEV-STAGING` and ongoing dev work; flagged below)
- `docs/README.md` (doc index)
- `WINDSURF.md` (mirror of CLAUDE.md for the Windsurf agent)
- `package.json` (Next 15, React 18, Stripe, Zustand, ShadCN/Radix; `dev` uses `--turbopack`; Jest configured but no real tests)
- `session-2026-02-04.md` (last session — 3 months stale)
- `docs/ghl-attribution/` — all 5 files (README.md, GHL_ATTRIBUTION_SESSION.md, FRONTEND-CONTRACT.md, GHL-WORKFLOW-CONTRACT.md, WP-PLUGIN-CONTRACT.md)
- `docs/architecture/` — all 5 files (overview, frontend, data-flow, routing, state-management)
- `docs/api/` — all 4 files (woocommerce, graphql, stripe, custom-endpoints)
- `docs/state/` — both files (zustand-stores, data-persistence)
- `docs/ui-ux/` — 4 of 5 (components, design-system, responsive-design, styling-conventions); `user-flows.md` is empty (0 bytes)
- `docs/guides/` — both files (getting-started, development-workflow)
- `docs/wp-plugins/` — both files (attribution-script.md, cyberize-attribution.php)
- Listed `docs/deployment/` — both files (`ci-cd.md`, `environments.md`) are empty (0 bytes)

## Discoveries / Drift Flags

### 1. Contract vs. Implementation drift in GHL attribution
The contract docs (`docs/ghl-attribution/README.md`, `FRONTEND-CONTRACT.md`, `WP-PLUGIN-CONTRACT.md`, `GHL-WORKFLOW-CONTRACT.md`) describe a `_dbx_` meta prefix, plugin name "DockBloxx GHL Attribution", slug `dockbloxx-ghl-attribution`, and sessionStorage keys prefixed `dbx_`. The actual implementation (`docs/wp-plugins/cyberize-attribution.php` and `GHL_ATTRIBUTION_SESSION.md`) uses:
- Plugin name: **Cyberize Attribution**
- Meta prefix: **`_coach_ghl_`**
- WP-side flags: `_cyberize_ghl_sent`, `_cyberize_ghl_sent_at`, `_cyberize_ghl_error`, `_cyberize_attribution_processed`
- sessionStorage keys (per `attribution-script.md`): unprefixed (`utm_source`, `utm_medium`, `coupon`, `landing_page`, etc.) — NOT `dbx_*` as the FRONTEND-CONTRACT specifies
- Coach's script uses `utm_term` in URL, mapped to `utm_keyword` form field; uses `gclid` (not `_cltk` as the Jan 27 session note claims)

**Implication:** Contract docs are stale. Implementation is reality. Do not trust the contract files as the source of truth — verify against `cyberize-attribution.php`, `attribution-script.md`, and the actual frontend `src/lib/attribution.ts` before any GHL-related changes.

### 2. README.md / directory-name conflict
Repo root `README.md` says: "this is the production build ... there will be no code change in this repo ... this is ready for deployment only". But the parent directory is `DEV-STAGING/dockbloxx-development-v1`, the working tree has uncommitted changes, and the Feb 4 session log treats this as the active dev environment. The README is misleading or stale; treat the directory name and session log as authoritative until Tony says otherwise.

### 3. Empty doc files (Tony may want these filled at some point)
- `docs/deployment/ci-cd.md` — 0 bytes
- `docs/deployment/environments.md` — 0 bytes
- `docs/ui-ux/user-flows.md` — 0 bytes

### 4. Pending work from Feb 4 session that may still be open
- Upload updated WP plugin to staging
- Update Coach's attribution script in WordPress ACF
- Deploy Next.js frontend to Vercel staging
- Test complete flow with real orders
- Edge case testing (direct traffic, partial UTMs, repeat orders, idempotency)
- Verify GHL stageId fix actually resolved the workflow error

## Project Snapshot (for quick recall)

- **Stack:** Next.js 15 (App Router, Turbopack), React 18, TypeScript 5, Tailwind 3.4, ShadCN/Radix, Zustand v5, native fetch (no SWR/React Query). Jest is installed but no real test suites yet.
- **External services:** WordPress GraphQL (blog content), WooCommerce REST (commerce), Stripe (payments), GoHighLevel (attribution → Opportunities via WP plugin webhook).
- **Endpoints source of truth:** `src/constants/apiEndpoints.ts`, all built from `NEXT_PUBLIC_BACKEND_URL` via `getApiUrl()`.
- **Custom Next API routes (`src/app/api/`):** `create-payment-intent`, `featured-products`, `get-all-post-slugs`, `get-all-posts`, `get-all-products`, `get-coupon-by-code`, `get-post-by-slug`, `get-product-by-slug`, `place-order`, `products-by-category`, `register-customer`, `search`, `update-order-status`.
- **Zustand stores (`src/store/`):** `useCartStore` (localStorage, partialize cartItems), `useCheckoutStore` (localStorage, partialize selected fields, `isHydrated` flag), `useProductStore` (IndexedDB via localforage), `usePaginationStore` (cursor), `useNumberedPaginationStore` (page-numbered, IndexedDB).
- **Route groups:** `(public)` is the only active group. `(admin)` and `(customers)` are placeholder layouts for future auth.
- **Checkout flow:** Stripe Elements + PaymentIntent → `/api/place-order` (creates Woo order with attribution as `_coach_ghl_*` meta) → Stripe `confirmPayment` → `/api/update-order-status` to "processing" → `woocommerce_order_status_processing` hook fires Cyberize plugin → POST to GHL inbound webhook → GHL workflow creates Contact + Opportunity.
- **Tony's hard rules (CLAUDE.md):** Plan mode mandatory before any edits; session file written before CLI display; scope discipline (touch only what's asked); push back on bad ideas; explanations precede code blocks; respect the Ironman Rule (don't break feature A while fixing feature B).

## Lessons Learned
- (none yet this session)

## End of Session State
- **Working:** Full GHL attribution pipeline reportedly fully operational as of Jan 30 (per session log). No new code touched today.
- **Broken:** Unknown — last reported issue (GHL stageId) had a fix proposed but not verified.
- **Next Steps:** Awaiting Tony's directive.

## Files Changed This Session
- `session-2026-05-07.md` — created
- `RECOVERY.md` — created (did not exist before)
