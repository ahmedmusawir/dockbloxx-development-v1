# Session Log: 2026-05-17

## Project Context
- **Project:** Dockbloxx (headless Next.js + WooCommerce storefront)
- **Tool:** Claude Code
- **Goal:** Finish the multi-day dep-security cleanup started 2026-05-16 — drive remaining 3 moderate vulns to **0**, prove the npm `overrides` pattern generalizes, expand `CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` (v0.2 → v0.5) into a production-ready App Factory artifact with end-to-end propagation workflow.

## Starting State
- **Branch:** `main`
- **Working tree:** Clean
- **Last commit:** `4cd1934 16may2026 - npm audit done and security playbook created` (yesterday's security pass landed cleanly)
- **Last Working Feature:** Full app per 1.0.0 release (May 11) + May 12 production layout fix + May 16 dep-security cleanup
- **Known Issues / Open Work at session open:**
  - 3 moderate npm-audit vulns remaining (from yesterday's pass):
    - `brace-expansion` — transitive, trivial, likely resolves with next dep refresh
    - `next` / `postcss` chain — needs a proper Next 15.x patch/minor upgrade plan. **DO NOT** run `npm audit fix --force` — proposes a Next 15 → 9.3.3 downgrade trap.
  - Zero critical, zero high vulns. Defensible posture.
  - *(All resolved during this session — see Session Progress below.)*

## Context Carried Forward From Yesterday (2026-05-16)
- Session arc: 25 vulns → `npm audit fix` (4) → axios removal (eliminated 18 CVEs from graph) → swiper@11.2.10 → swiper@12.1.4 → **3 moderate**.
- `agent_docs/CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` v0.2 now exists — principles P1–P7, two complete case studies.
- New principle P7 (Require visual sign-off before finalizing customer-facing upgrades) was promoted from Tony's pause-at-step-4 workflow modification during the swiper migration.
- Skill-extraction candidates noted in playbook for App Factory rollup: `/audit-deps`, `/check-dep-usage`, `/remove-unused-dep`, `/plan-breaking-upgrade`, `/security-audit-report`.
- Memories updated yesterday: security workflow validated; Dockbloxx work doubles as App Factory playbook source.

## Session Progress

### COMPLETE — Resolve postcss CVE via npm `overrides`
**Task:** Clear the moderate postcss CVE without a risky Next.js major upgrade.

**Investigation findings (the punchline):**
- Already on `next@15.5.18` — the latest stable Next 15.x patch (15.6.x does not exist; 16.x is the next major)
- `next@15.5.18`, `next@16.0.0`, `next@16.2.6` all pin `postcss@8.4.31` exact
- **Upgrading Next does not move postcss** — the "next upgrade to clear postcss" plan from yesterday was a dead end as written
- npm `overrides` is the correct escape hatch

**Plan file:** `/home/moose/.claude/plans/cozy-weaving-wolf.md` (overwritten from yesterday's swiper plan)

**Execution:**
| Step | Result |
|------|--------|
| 1. Add `overrides` block to `package.json` | Added `"postcss": "^8.5.10"` |
| 2. `npm install` (first attempt) | ❌ `EOVERRIDE` — direct devDep `postcss@^8.4.38` conflicted with override range. Bumped direct dep to `^8.5.10`. Reran. |
| 2. `npm install` (second attempt) | ✅ `removed 1 package, changed 1 package`, audit immediately drops 3 → 1 moderate |
| 3. `npm ls postcss` | All 9 instances resolve to `8.5.14` (including `next@15.5.18 → postcss@8.5.14 deduped`). Override marker confirmed on the top-level entry. |
| 4. `npm audit` | **1 moderate** (`brace-expansion` only). postcss fully resolved. |
| 5. `npm run build` | Exit 0. Same bundle sizes as pre-change. No CSS pipeline regression in build output. |
| 6. Dev-server sanity check | All three pages (`/`, `/shop/life-saver`, `/cart`) HTTP 200, stylesheet bundles present (1 per page), response sizes healthy (531KB / 378KB / 137KB). |
| 7. Bookkeeping | This file + CHANGELOG + RECOVERY + playbook Case Study #3 + new principle P8 |

**Compounded session arc (across both calendar days):**
| Snapshot | Total | Critical | High | Moderate |
|----------|------:|---------:|-----:|---------:|
| 2026-05-16 start | 25 | 3 | 11 | 9 |
| After yesterday's pass (axios + swiper) | 3 | 0 | 0 | 3 |
| **After today's postcss override** | **1** | **0** | **0** | **1** |

**Lessons Learned**

- Sometimes the "upgrade to fix" path doesn't actually fix anything — always verify the bundled transitive version of the would-be-upgrade target *before* committing to the upgrade. (`npm view <pkg>@<version> dependencies.<dep>` is the one-liner.)
- npm `overrides` requires the direct dep range to intersect the override range; if you get `EOVERRIDE`, bump the direct dep to match before retrying. This is a non-obvious gotcha for first-time `overrides` users.
- A "moderate XSS via CSS stringify" advisory in a build-time-only dep is a CVE-hygiene play, not a real-risk fix — but doing it cleanly via overrides costs ~10 min and gives a clean audit posture for any future security gate, so it's worth doing.
- The override applied to **every** nested postcss instance in the dep tree, including Next's. npm's override resolution is more powerful than people sometimes assume — it's not just for top-level deps.

### COMPLETE — Apply same override pattern to `brace-expansion`
**Task:** Second application of P8 to prove the override pattern generalizes beyond postcss.

**Execution:**
- Added `"brace-expansion": "^2.0.2"` to existing `overrides` block (one-line addition)
- `npm install`: clean on first try (no `EOVERRIDE` — brace-expansion isn't a direct dep). Net `-8 packages` from dedup.
- `npm ls brace-expansion`: all 8 instances → `2.1.0` (eslint→minimatch@3 marked `overridden`, rest `deduped`)
- `npm audit`: **`found 0 vulnerabilities`**
- `npm run build`: exit 0, bundle sizes unchanged
- `npm run test` (Jest): **166/166 pass across 15 suites** in 3s
- `npm run test:e2e` (Playwright): **16/16 pass** in 59.6s
- CHANGELOG: added Security entry above the postcss entry
- `agent_docs/CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` v0.3 → v0.4: added Case Study #4 documenting the second P8 application + updated playbook-internal changelog

**What this proves:** P8 (npm `overrides` over major-version upgrade when parent pins vulnerable transitive) generalizes across parent ecosystems — worked identically for Next/postcss (framework internal) and eslint/brace-expansion (tooling internal). The `EOVERRIDE` failure mode from Case Study #3 is *case-specific* (only when target is also declared as direct dep), not P8-general.

### COMPLETE — Afternoon: Three-Repo Promotion (dev → local prod mirror → Vercel prod)
**Task:** Propagate the 0-vuln dep tree from the dev repo through to live production via the three-phase procedure later codified as P9/P10/P11/P12 + the Three-Repo Promotion procedure section in the playbook.

**Phase 1 — Dev repo (source of truth):**
- Already complete from morning work (postcss + brace-expansion overrides applied, tests + build green, 0 vulns)
- `package.json` + freshly-regenerated `package-lock.json` are the propagation artifacts

**Phase 2 — Local prod mirror (real prod backend, local code):**
- Confirmed `.env.local` points at real production Pressable/WooCommerce backend (non-negotiable pre-flight)
- Copied `package.json` + `package-lock.json` from dev repo
- `rm -rf node_modules && npm ci` — strict install per the lockfile
- `npm audit`: 0 vulns (matches dev exactly — proof the lock-based propagation is deterministic)
- `npm run build` + `npm test` + `npm run test:e2e`: all green
- Manual smoke vs real prod backend per `MANUAL_SMOKE_TEST.md`: Stripe test card 4242 4242 4242 4242, test order deleted from prod admin post-smoke
- All green → proceed to Vercel

**Phase 3 — Vercel prod repo:**
- Pre-flight: confirmed inside repo folder (no stray parent-level files per G-NPM-3)
- Copied `package.json` + `package-lock.json`
- `npm ci` clean
- Audit + build + tests all green
- Committed and pushed → **`git push` rejected: divergent branches**
- Applied P12 diagnostic procedure:
  - `git log HEAD..origin/main --oneline` → remote had pre-existing privacy + MeetTheTeam content commits from a separate workflow
  - `git log origin/main..HEAD --oneline` → local: just package.json + package-lock.json changes
  - `git show --stat <remote-commits>` → remote commits touched `src/app/...` content pages, NOT package files
- Verdict: safe to `git pull --rebase origin main && git push` (no overlap with our changed files)
- Push succeeded → Vercel auto-deploy triggered
- Post-deploy smoke against live URL: all clean per MANUAL_SMOKE_TEST.md
- Watched first 15 minutes of production traffic: zero errors, zero regressions reported

**Outcome:** **0 vulnerabilities deployed live to production.** First fully-documented multi-repo security promotion in App Factory history. The divergence event in Phase 3 became the empirical seed for principle P12.

### COMPLETE — Playbook v0.5 → v0.6 (Three-Repo Promotion codified)
**Task:** Capture the afternoon's three-repo promotion as a load-bearing case study, formalize the procedure as a top-level section, codify new principles + gotchas observed during execution.

**Restructure applied to `agent_docs/CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` (v0.5 → v0.6):**
- File renamed `CYBER_REPO_SECURITY_PLAYBOOK_v0.5.md` → `CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` (per Tony's filename-convention rule + cross-references updated in 8 places: CHANGELOG, RECOVERY, both session files, 2 memories, plan file, the playbook itself)
- **Case Studies restructured:** old standalone #3 (postcss) and #4 (brace-expansion) folded into a new unified **Case Study #3 — Three-Repo Promotion: From 25 Vulns to Zero, End-to-End** as sub-parts #3a and #3b, with the afternoon's three-repo arc as the umbrella narrative (threat-landscape context, Day 1 reference, morning work, afternoon promotion timeline, lessons-learned-specific-to-multi-repo, retroactive playbook updates)
- **Replaced v0.5's P9** (generic "lockfile + npm ci + eyeball") with the more specific **P9 — Three-Repo Promotion for Customer-Facing Security Patches** (explicit three-phase model with hard checkpoints)
- **Added P10** — `npm ci` for Lock-Strict Promotion Between Repos (use install when establishing the lock, ci when propagating it)
- **Added P11** — Threat-Landscape Check Before Every Install Session (60-second supply-chain check; Mini Shai-Hulud + node-ipc cited as reference incidents)
- **Added P12** — Git Divergence: Investigate Before Pulling, Never Force (three diagnostic commands → decision matrix; origin: today's prod repo divergence)
- **New top-level section: Procedure: Three-Repo Security Patch Promotion via npm ci** — the load-bearing runbook with command-by-command Phase 1 / 2 / 3 + checkpoints + "why install in Phase 1 but ci in Phases 2+3" comparison table + quick-reference card
- **New top-level section: Gotchas** — three named failure modes:
  - **G-NPM-1** — `EOVERRIDE` (overrides must intersect direct-dep range; bump direct dep range to match)
  - **G-NPM-2** — `rm -rf .next` before final manual smoke (caches reference old dep tree; routesManifest.dataRoutes error)
  - **G-NPM-3** — stray parent-level files trigger Next.js multi-lockfile warning
- **Phase 6 in Standard Workflow** reduced to a thin pointer at the new Procedure section
- **Companion Documents updated:** `MANUAL_SMOKE_TEST.md` (repo root) added with explicit relationship-to-Eyeball-Checklist explanation
- **Appendix A canonical-example callout updated:** now describes Case Studies #1 → #3 (was #1 → #4) with the new Case Study #3 framed as "the load-bearing chapter that captures the full local → mirror → production arc"
- **Internal Changelog table** added row v0.6 with full restructure summary

### COMPLETE — CHANGELOG promotion to [1.1.0]
**Task:** Promote `[Unreleased]` → `[1.1.0] - 2026-05-17`; add new empty `[Unreleased]` above.

**Done:**
- New empty `[Unreleased]` section at top
- `[1.1.0] - 2026-05-17` with summary header capturing the full two-day arc
- Consolidated the two `### Added` subsections into one (Keep a Changelog convention: one per category per version)
- Order within [1.1.0]: Added (playbook v0.6, v0.5, v0.1 history, Testing Playbook v2.0) → Security (brace-expansion, postcss, swiper) → Removed (axios)
**Task:** Codify Tony's production-deployment workflow (copy package.json + lockfile → `npm ci` → build → manual eyeball) into the playbook as a reusable end-to-end process. Mark Case Studies #1→#4 as the canonical reference walkthrough for App Factory replication.

**Added to `agent_docs/CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` (v0.4 → v0.5):**
- **New Phase 6** (Production propagation + pre-deploy verification): 5 sub-steps (6a propagate artifacts, 6b `npm ci` install with full `install` vs `ci` comparison table, 6c production build verify with the `routesManifest.dataRoutes` failure-mode + `rm -rf .next` fix documented, 6d eyeball, 6e deploy).
- **New section: Pre-Deployment Eyeball Checklist** — reusable, scoped to customer-facing storefronts. 8 pages (homepage, single product, cart, checkout, category, prose/typography, search, dealer-coupon), each with viewport-specific bullets. Cross-cutting checks (console errors, network 404s, CLS, hover states, shadows, fonts). 5-minute speed-run order. "What to do if eyeball fails" section.
- **New principle P9** — production deployment requires lockfile propagation + `npm ci` + pre-deploy eyeball. Anchors Phase 6.
- **Canonical-example callout** at the top of Appendix A marking Case Studies #1→#4 as the reference end-to-end walkthrough for new operators.
- Playbook changelog row v0.5.

**Why this matters:** The playbook is now ready for App Factory replication. A new agent (or human operator) coming to repo-dep-security work fresh can read Case Studies #1→#4 + the Standard Workflow + Phase 6 + the eyeball checklist and have a complete worked example end-to-end — local audit through production deploy.

## End of Session State
- **Working:** All previously working features intact. Build clean. Full test suite green (Jest 166/166, Playwright 16/16). Manual smoke passed in all three repos (dev, local prod mirror, Vercel prod). Live production deploy clean — first 15 min of traffic post-deploy showed zero regressions.
- **Broken:** Nothing.
- **Security posture:** **`npm audit: found 0 vulnerabilities`** — and this is now true in **production**, not just dev. Two-day session arc: **25 vulns → 0 deployed live** (1 case study #3 with sub-parts #3a/#3b spanning the morning and afternoon, plus referenced #1 + #2 from yesterday).
- **Playbook state:** `agent_docs/CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` **v0.6** — production-verified for App Factory replication. End-to-end workflow codified including the load-bearing **Three-Repo Promotion procedure** (dev → local prod mirror → Vercel prod with command-by-command runbook). Principles P1–P12 (P9 replaced + P10–P12 added in v0.6). Three case studies in the canonical reference walkthrough. Three named gotchas (G-NPM-1, G-NPM-2, G-NPM-3). MANUAL_SMOKE_TEST.md companion document linked.
- **CHANGELOG:** `[Unreleased]` cleared; `[1.1.0] - 2026-05-17` released captures the full two-day arc (4 dep changes + playbook seed-through-v0.6 + Testing Playbook v2.0).
- **Working tree:** dirty per no-commit rule. Modified files listed below.
- **Next step (Tony's planned action):** Review the v0.6 playbook + 1.1.0 CHANGELOG; commit when satisfied.
- **Next steps (optional, separate sessions):**
  1. Consider promoting `agent_docs/CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` v0.6 workflow chunks into Claude Code skills (`/audit-deps`, `/check-dep-usage`, `/remove-unused-dep`, `/plan-breaking-upgrade`, `/security-audit-report`, plus possibly `/promote-security-patch` for the three-repo procedure) once a second App Factory project (StarkReads etc.) exercises the same pattern.
  2. Apply the playbook to other App Factory repos and gather generalization signal before locking the workflow into skills.

## Files Changed This Session

**Dev repo (this repo):**
- `package.json` — added `overrides` block (`postcss: ^8.5.10`, `brace-expansion: ^2.0.2`); bumped direct `postcss` devDep `^8.4.38` → `^8.5.10`
- `package-lock.json` — regenerated multiple times (postcss override, brace-expansion override, lockfile-fresh-resolution rebuild); net `-9 packages` via dedup
- `CHANGELOG.md` — promoted `[Unreleased]` → `[1.1.0] - 2026-05-17` with summary header capturing the full two-day arc; consolidated `### Added` subsections; new empty `[Unreleased]` at top
- `RECOVERY.md` — pointer updated through each task; current state reflects production deploy and v0.6 playbook
- `session_2026-05-17.md` — this file (populated incrementally; afternoon promotion + v0.6 expansion appended)
- `agent_docs/CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md` — **renamed** from `_v0.5.md`; versioned progression **v0.2 → v0.6** across the session:
  - **v0.3:** added (now-folded) Case Study #3 (postcss) + new principle P8
  - **v0.4:** added (now-folded) Case Study #4 (brace-expansion) — second P8 application
  - **v0.5:** added Phase 6 (now reduced to pointer) + reusable Pre-Deployment Eyeball Checklist + (now-superseded) P9
  - **v0.6:** **major restructure** — old Case Studies #3 + #4 folded into unified **Case Study #3 — Three-Repo Promotion** (with #3a/#3b sub-parts + afternoon arc); replaced v0.5's P9 with three-repo P9; added P10/P11/P12; new **Procedure: Three-Repo Security Patch Promotion via npm ci** section; new **Gotchas** section (G-NPM-1, G-NPM-2, G-NPM-3); MANUAL_SMOKE_TEST.md added to Companion Documents

**Other repos (afternoon promotion — NOT in this dev repo):**
- Local prod mirror repo: copied `package.json` + `package-lock.json` from dev → `npm ci` → tests + build + smoke green
- Vercel prod repo: copied same artifacts → `npm ci` → tests + build green → committed → push (initial reject due to divergence) → `git pull --rebase` → push succeeded → Vercel auto-deploy

**Memory updates (in `~/.claude/projects/.../memory/`):**
- `feedback_security_workflow_validated.md` — cross-reference path updated to v0.6
- `project_app_factory_playbook_output.md` — cross-reference path updated to v0.6

## Plan files (in `~/.claude/plans/`)
- `cozy-weaving-wolf.md` — overwritten today for the postcss override plan (yesterday's swiper plan was previously here); cross-reference path updated to v0.6

---

## Session boundary note

The **production deploy** that this session captures completed 2026-05-17 afternoon. The **codification work** (playbook v0.5 → v0.6 restructure + CHANGELOG 1.1.0 promotion) was initiated under this session's context but completed early 2026-05-18 — see [session_2026-05-18.md](./session_2026-05-18.md) for the continuation. The [1.1.0] release tag correctly reflects the deploy date (2026-05-17), not the documentation date.
