# Session Log: 2026-06-11

## Project Context
- **Project:** Dockbloxx (headless Next.js + WooCommerce storefront)
- **Tool:** Claude Code
- **Goal:** TBD — session file created at start of day per CLAUDE.md mandatory protocol. Task will be captured here once Tony states it.

## Starting State
- **Branch:** `main`
- **Last commit:** `9e64679 18may2026 - final security playbook`
- **Working tree:** Dirty carryover from prior sessions. Per `git status` snapshot at session open:
  - Modified: `package-lock.json`
  - Deleted (staged): `MANUAL_SMOKE_TEST.md`, several `agent_docs/*.md` files (testing playbook v1.0/v2.0 drafts, playbook notes, recon, unit-test audit), and four `session_2026-05-*.md` files
  - Untracked: `.env.local.example`
- **Last working feature:** Full app per 1.0.0 + May 12 production layout fix + May 16–18 dep-security cleanup. **Production deployed clean** (0 vulnerabilities) since 2026-05-17 afternoon.
- **Security posture:** 0 vulnerabilities live in production. Three-repo promotion procedure codified in `agent_docs/CYBER_REPO_SECURITY_PLAYBOOK_v0.6.md`.
- **Known issues / Open work:** None reported. ~3-week gap since last session (`session_2026-05-18.md`). Working tree's deleted-file set suggests a cleanup/reorg that has not yet been committed — flag to Tony before touching.

## Context Carried Forward From 2026-05-18
- Two-day security arc (25 vulns → 0) shipped to production 2026-05-17; codified in playbook v0.6 + CHANGELOG [1.1.0] on 2026-05-18.
- Open optional next steps from that session (carry forward only if Tony picks them up):
  1. Apply the playbook to a second App Factory project to gather generalization signal
  2. Extract workflow chunks into Claude Code skills (`/audit-deps`, `/check-dep-usage`, `/remove-unused-dep`, `/plan-breaking-upgrade`, `/promote-security-patch`)

## Session Progress

### COMPLETE (FILE WRITTEN) — `scripts/seed-dealers.mjs` did not seed — Tony entered data manually instead
**Task:** Write (do NOT run, do NOT commit) a one-shot Node CLI script that pushes the 15 existing dealers from `src/app/(public)/dealer-locator/data.js` into the dev-staging WP page (slug `dealer-locator`) `acf.dealer_data` ACF repeater via REST. Tony runs it locally, supplying creds via `WP_USER` + `WP_APP_PW` env vars.

**Key research findings:**
- `data.js` exists with 15 rows, exported as `export const dealers = [...]` (ESM syntax)
- `package.json` has no `"type": "module"` → Node treats `.js` as CJS by default → a naive `import from '../src/.../data.js'` from the `.mjs` script would **fail at parse time** (the `export const` keyword is ESM-only)
- `scripts/` directory exists, but no existing `.mjs` script — only `.sh` and `.ts`
- Node v24.15.0 — native `fetch` available, no extra deps needed
- Route group `(public)` is literal in the path; Node handles parens in paths fine, no shell concern

**Proposed design (one ambiguity to flag):**
1. Read `data.js` file as text, then dynamic-import it via a `data:text/javascript;base64,...` URL. This side-steps the CJS/ESM mismatch with no temp files, no source-tree changes, no rename of `data.js`. **Alternative considered:** write to an `os.tmpdir()` `.mjs` file and import that — slightly more code, same result. **Rejected:** renaming `data.js` → `data.mjs` (out of scope; would touch Next consumers).
2. Resolve page ID: `GET {WP}/wp-json/wp/v2/pages?slug=dealer-locator` → `[0].id`. Bail with clear error if empty.
3. Map keys: `name→dealer_name`, `address→dealer_address`, `phone→dealer_phone`, `website→dealer_web_url`.
4. Write: single `POST {WP}/wp-json/wp/v2/pages/{id}` with body `{ acf: { dealer_data: [...mapped] } }`. Overwrite semantics, fully idempotent.
5. Auth: `Authorization: Basic base64(WP_USER:WP_APP_PW)`. Both env vars required; bail clearly if missing.
6. On non-OK response, throw with status + body text.
7. Success log: count of rows written.

**Files to create:**
- `scripts/seed-dealers.mjs` — the seeder script

**Files NOT touched:**
- `src/app/(public)/dealer-locator/data.js` — source of truth, read-only
- `package.json` — no `"type": "module"` change, no new dep
- Anything else

**Run command (will be a top-of-file comment):**
```
WP_USER='your-user' WP_APP_PW='xxxx xxxx xxxx xxxx xxxx xxxx' node scripts/seed-dealers.mjs
```

**Assumptions:**
1. Native ACF REST write accepts `POST /wp/v2/pages/{id}` with `{acf: {dealer_data: [...]}}` (Tony confirmed `acf` key is present in the page response, which is the ACF-to-REST signature).
2. WP base URL hardcoded as `https://dockbloxx.mystagingwebsite.com` in the script (dev-staging only — this is a one-shot seed; not for prod).
3. App Password is passed exactly as WP issues it (with spaces). Basic-auth base64 includes them as-is.

**Risks:**
- If the ACF REST plugin is configured for separate ACF endpoints (not native), the `POST /wp/v2/pages/{id}` shape would be wrong. Tony's note "Native ACF REST write is enabled (the `acf` key is present)" mitigates this.

**Status:** File written, ran clean (`Seeded 15 dealers into page #12595`, exit 0), but the WP-side outcome was effectively a no-op — the `dealer_data` repeater did not receive the 15 rows. The single populated row Tony saw in the `GET /wp-json/wp/v2/pages?slug=dealer-locator` response was a manual test entry he had placed earlier, not output from the script. My initial read of that response (attributing dealer 1's presence to the script) was wrong — Tony corrected.

**Root cause (now confirmed empirically on this WP install):** native WP REST `acf` key write at `POST /wp/v2/pages/{id}` with `{acf:{dealer_data:[...]}}` is **silently no-op for ACF repeater fields** on dockbloxx.mystagingwebsite.com. The endpoint accepts the request and returns 200, `_acf_changed` stays `false`, and the repeater is not updated. The `acf` key being present in GET responses confirms READ integration only, not write support. This install does not expose the `/wp-json/acf/v3/...` (ACF to REST API plugin) routes — would have to be installed if a future script-based seed is wanted.

**Resolution:** Tony entered all 15 rows manually in WP admin (faster than diagnosing the plugin/endpoint situation). Data is now live in the repeater.

**Files changed (still on disk):**
- `scripts/seed-dealers.mjs` — **NEW** (~67 lines). Working as written for its target shape; the shape itself is the wrong one for this install. Leaving in place per scope discipline — Tony can delete it, repurpose it once the `/acf/v3/...` plugin is installed, or rewrite the body shape if a future seed is needed.

**Not committed** per Tony's instructions.

**Lesson (saved to memory):** On this WP install, ACF repeater writes via the native `acf` key are silently rejected. Future ACF-write scripts must use the ACF to REST API plugin (`POST /wp-json/acf/v3/pages/{id}` with `{fields:{...}}`) or WP-CLI directly — and the plugin must be confirmed installed BEFORE assuming the shape will work, since `acf`-in-GET-response doesn't imply write support.

### COMPLETE — Add `DEALER_LOCATOR_REST_URL` constant
**Task:** Append one export to `src/constants/apiEndpoints.ts` composing the dealer-locator slug-query URL from the existing `WP_REST_PAGES` constant. Exact text supplied by Tony. Plan Mode skipped — this was a one-line addition with the verbatim text given and no design ambiguity (CLAUDE.md one-line-typo carve-out + "write and stop for review" instruction).

**Done:**
- Read `apiEndpoints.ts` to confirm `WP_REST_PAGES` exists (line 5) and capture style
- Appended one line after the last existing export (no blank-line separator — matches file's existing density)
- Used the template-literal composition form per spec, not `getApiUrl()` — composing from `WP_REST_PAGES` (the existing constant) avoids hardcoding the WP base path twice

**Files changed:**
- `src/constants/apiEndpoints.ts` — +1 line: `export const DEALER_LOCATOR_REST_URL = \`${WP_REST_PAGES}?slug=dealer-locator\`;`

**Not committed** per Tony's instructions.

### COMPLETE — Create `src/types/dealer.ts`
**Task:** Create the app-side clean shape for a dealer — four string fields with app naming (`name`, `address`, `phone`, `website`), deliberately NOT WP's `dealer_name`/etc. Match existing `src/types/` conventions.

**Convention recon:** Reviewed `dealer-coupon.ts`, `videos.ts`, `page.ts`, `coupon.ts`. Dominant idiom: `export interface X { ... }`, 2-space indent, double quotes, trailing semicolons. JSDoc headers used for non-trivial WP-shape types (`page.ts`); omitted for simple shapes (`dealer-coupon.ts`, `videos.ts`). No file-level comments for trivial types. Matched the `dealer-coupon.ts` no-comment style — there's no non-obvious WHY to document.

**Files changed:**
- `src/types/dealer.ts` — **NEW** (6 lines): `Dealer` interface with `name`, `address`, `phone`, `website` — all `string`.

**Not committed** per Tony's instructions.

### COMPLETE — Append `getDealers()` to `src/services/dealerServices.ts`
**Task:** Add the dealer-locator service function `getDealers(): Promise<Dealer[]>` — fetch the slug-queried WP page with `&_fields=acf`, read `data[0].acf.dealer_data`, map WP names to clean `Dealer` shape, return `[]` on any failure.

**Confusion management trigger (Path A vs B vs C):** The target filename already existed and housed the **dealer-coupon** feature (`fetchAllDealerSlugs`, `fetchDealerPageData` — both used by `src/app/(public)/dealer-coupon/[dealerSlug]/page.tsx`). Stopped to surface the conflict. Tony picked **Path A — append + isolate via detailed section comment** to keep all dealer-related services in one file.

**Done:**
- Recon: read `categoryServices.ts` and existing `dealerServices.ts` for style. Existing file uses `export async function`, try/catch with `console.error("[fn] Error:", error)`, safe-default return on error, `next: { revalidate: N }`, no JSDoc.
- Imports: combined existing single-symbol import of `DEALER_REST_COUPONS` into a multi-line `{ DEALER_LOCATOR_REST_URL, DEALER_REST_COUPONS }` from `@/constants/apiEndpoints` (alphabetical). Added `import { Dealer } from "@/types/dealer"`.
- Appended a 20-line section banner comment marking the dealer-locator boundary: explains the distinct WP source, the data path `data[0].acf.dealer_data`, the WP-naming quarantine rule (`dealer_name`/etc. must not leak past this file), the `&_fields=acf` performance trim, and the `[]`-on-any-failure contract.
- Added `getDealers()` matching all 7 task requirements: fetch `${DEALER_LOCATOR_REST_URL}&_fields=acf`, `revalidate: 30`, defensive optional-chaining + `Array.isArray` check, inline-typed mapper with `?? ""` per-field default, try/catch returning `[]`, `console.error("[getDealers] Error:", error)`.
- Pre-existing functions (`fetchAllDealerSlugs`, `fetchDealerPageData`) untouched.

**Files changed:**
- `src/services/dealerServices.ts` — +57 lines (2-line import refactor + 20-line section comment + 30-line `getDealers` function + blank-line separator). Pre-existing functions byte-identical.

**Not committed** per Tony's instructions.

### COMPLETE — Wire `DealerLocatorContent.tsx` to live data
**Task:** Replace the static `import { dealers } from "./data"` with `const dealers = await getDealers()` from the live service. Minimal change. Keep Server Component. Leave `data.js` on disk as fallback until Tony eyeballs the page.

**Recon:**
- `page.tsx` is a thin server-side wrapper: `<DealerLocatorContent />`, no awaits, no props.
- `DealerLocatorContent.tsx` was already a Server Component — no `"use client"`, no React hooks (no useState/useEffect/etc.), just JSX with `dealers.map(...)` consuming `dealer.name`, `dealer.address`, `dealer.phone`, `dealer.website` — byte-perfect match for the `Dealer` interface.
- Nothing forced a bigger change. Requirement #6 not triggered.

**Two edits applied (only to `DealerLocatorContent.tsx`):**
1. Import swap: `import { dealers } from "./data";` → `import { getDealers } from "@/services/dealerServices";`
2. Function body: `() => {` → `async () => {`, plus `const dealers = await getDealers();` before the `return (` (with a blank line for breathing room — matches surrounding style).

**Intentionally NOT touched:**
- `.map()` block and all JSX/markup/styling — Tony's spec said unchanged.
- `page.tsx` — no need to fetch upstream and pass as prop; the async-Server-Component-fetches-at-the-top pattern is more minimal and matches Tony's stated preference.
- `data.js` — left on disk as visual-verification fallback, per Tony's spec. The new import path no longer references it but the file is still there.
- The pre-existing `<Head>` usage from `next/head` (App Router project; canonical idiom would be `export const metadata`) — orthogonal Pages-Router-residue; not in scope.

**Files changed:**
- `src/app/(public)/dealer-locator/DealerLocatorContent.tsx` — import line swapped (1 line), function signature made async, one `const dealers = await getDealers();` line added at top of body.

**Pending verification:** Tony loads the page, confirms all 15 dealers render from WP, gives visual sign-off → then a final cleanup prompt deletes `data.js`.

**Not committed** per Tony's instructions.

**Visual sign-off:** ✅ Tony loaded the page, all 15 dealers rendered "perfect." Live data is confirmed working.

### COMPLETE — Add client-side substring search to dealer-locator page
**Task:** Add a case-insensitive substring search input over `dealer.name` to the dealer-locator page. Server fetch stays put; filter is client-side over the already-fetched array. Architecture: Server Component holds `await getDealers()` and passes `dealers: Dealer[]` to a new `"use client"` component that owns the search state.

**Recon (no changes yet):**
- `DealerLocatorContent.tsx` currently: server component, `await getDealers()` at top, renders hero + `Page>Row` containing `<h2>The Dealer List</h2>`, subtitle `<p>`, then a `<div className="grid ...">` of `dealers.map((dealer, index) => <div ...card...>)` blocks.
- The card markup is a clean self-contained chunk — three react-icons + four `Dealer` fields, no `Image`, no async, no hooks, no parent-state dependency. Trivial to lift into a client component verbatim. Requirement #6 (bail-out for tangled markup) NOT triggered.
- The three `react-icons/md` imports (`MdOutlinePhone`, `MdOutlineLocationOn`, `MdOutlineLanguage`) are used ONLY inside the card block. Once the cards move, those imports become unused in the server component and should be removed (direct consequence of the refactor, not unsolicited cleanup).

**Status:** Approved with one change → executed → both files written.

**Tony's correction:** Use `key={dealer.name}` instead of `key={index}` on the filtered cards. **My plan had it wrong** — I'd kept `key={index}` to "mirror existing pattern" and defended it on the basis that pure render-from-props handles stale-node reuse fine. Tony flagged this as a real correctness issue: index keys on a filtered/dynamic list cause stale-node reuse and flicker as the filter changes (focus state, transitions, DOM transient state). **Crucially, Tony scoped the fix** — "correctness fix scoped to the new search behavior, not a codebase-wide idiom change." Don't sweep through other `key={index}` usages elsewhere in the codebase. Applied only to the new `DealerList.tsx`.

**Done:**
- `src/app/(public)/dealer-locator/DealerList.tsx` — **NEW** (~75 lines): `"use client"`, `useState("")` for the query, `dealers.filter((d) => d.name.toLowerCase().includes(query.toLowerCase()))`, centered `max-w-md` input with `rounded-lg shadow-sm focus:ring-blue-500`, empty-state `<p className="text-gray-600">No dealers match your search.</p>`, card markup lifted verbatim from the server component **with `key={dealer.name}` per Tony's correction**.
- `src/app/(public)/dealer-locator/DealerLocatorContent.tsx` — server fetch UNTOUCHED (still `async`, still `await getDealers()`). Removed the three now-unused `react-icons/md` imports. Added `import DealerList from "./DealerList";`. Replaced the entire grid `<div className="grid ...">{dealers.map(...)}...</div>` block with one line: `<DealerList dealers={dealers} />`. Hero, `Page>Row`, `<h2>`, subtitle `<p>`, both `<Head>` blocks — all byte-identical.

**Files changed:**
- `src/app/(public)/dealer-locator/DealerList.tsx` — NEW client component
- `src/app/(public)/dealer-locator/DealerLocatorContent.tsx` — icon imports removed, `DealerList` import added, grid replaced with `<DealerList dealers={dealers} />`

**Not committed** per Tony's instructions.

**Visual sign-off:** ✅ Tony confirmed search UX is perfect.

### COMPLETE — Inject `foundingDate` + `founder` into homepage JSON-LD Organization node
**Task:** Add a `injectOrganizationFacts(schema)` helper in `src/lib/seoUtils.ts` that takes the PARSED schema object, finds the Organization node in `@graph`, and adds `foundingDate: "2022"` + `founder: { "@type": "Person", "name": "Brady Bragg" }`. Must match BOTH `@type === "Organization"` AND `Array.isArray(@type) && @type.includes("Organization")`. Wire into `src/app/(public)/page.tsx` between the schema-object access and `JSON.stringify` — order: **object → inject → stringify → fixUrl**. Homepage ONLY; other page.tsx files (about/shop/contact/etc.) reference Organization by `@id` and must NOT receive duplicated founder data.

**Status:** Approved → executed → both files written.

**Done:**
- `src/lib/seoUtils.ts` — appended `injectOrganizationFacts(schema)` (exported) + `isOrganizationNode(node)` (file-private). JSDoc header on `injectOrganizationFacts` explains BOTH the parse-time-not-string-time requirement AND the array-vs-string `@type` matching. `fixUrl` and all fetchers untouched.
- `src/app/(public)/page.tsx` — extended `seoUtils` import to multi-line including `injectOrganizationFacts`. Insertion between schema access and `JSON.stringify`: `const withFacts = yoast?.schema ? injectOrganizationFacts(yoast.schema) : null;` then `let schema = withFacts ? JSON.stringify(withFacts) : null;`. `fixUrl(schema)` line below is untouched — still runs on the stringified output, no behavior change. `generateMetadata`, the `Script` tag, `HomePageContent`, the `beforeInteractive` strategy — all unchanged.

**Transform order confirmed end-to-end:**
1. `yoast.schema` (parsed object) → `injectOrganizationFacts(...)` mutates and returns
2. → `JSON.stringify(...)` serializes the augmented object
3. → `fixUrl(...)` replaces backend URLs in the string (no founder/founding fields contain URLs, so this step's behavior is identical)

**Files NOT touched (per spec — scope-discipline):**
- All other page.tsx files in `src/app/(public)/**` — about, shop, contact, dealer-locator, blog, every sub-route. Organization is referenced there by `@id`; duplicating founder data would corrupt the JSON-LD linked-data graph.
- `fetchYoastSEOJson`, `fixUrl`, `mapYoastToMetadata`, all WC/WP fetchers in seoUtils — unchanged.
- The `<Script id="yoast-schema-moose">` block, `strategy="beforeInteractive"`, `HomePageContent` — unchanged.

**Files changed:**
- `src/lib/seoUtils.ts` — appended ~35 lines (one exported function + one private helper + JSDoc)
- `src/app/(public)/page.tsx` — import refactored to multi-line + 2-line schema-access change

**Not committed** per Tony's instructions.

**Visual sign-off:** ✅ Tony confirmed JSON-LD test passed.

### COMPLETE — Delete `src/app/(public)/dealer-locator/data.js`
**Task:** Delete the now-unused static fallback. Precondition gate: grep first; if anything still imports it, STOP. If clean, delete and confirm the page still builds.

**Verification before delete:**
- `grep -rn 'dealer-locator/data\|dealer-locator/./data' src/` → 0 hits
- `grep -rn 'from "./data"' src/app/(public)/dealer-locator/` → 0 hits
- `grep -rn 'data\.js' src/` → 0 hits
- `grep -rn 'dealer-locator/data\|from "./data"' tests/` → 0 hits
- The two consumers from earlier sessions (`DealerLocatorContent.tsx` via prior live-data swap, `DealerList.tsx` brand-new from the search task) both import from the service / type — not from the static file. No remaining references anywhere.

**Verification after delete:**
- `ls src/app/(public)/dealer-locator/` → 3 files remain: `DealerList.tsx`, `DealerLocatorContent.tsx`, `page.tsx`. `data.js` is gone.
- `npx tsc --noEmit` → exit 0 with zero output. The whole project type-checks, dealer-locator page included.

**Done:**
- `rm src/app/(public)/dealer-locator/data.js` — file deleted

**Files changed:**
- `src/app/(public)/dealer-locator/data.js` — **DELETED**

**Not committed** per Tony's instructions.

### COMPLETE — Unit tests for getDealers / search filter / injectOrganizationFacts
**Task:** Write unit tests for the three pieces of logic built today. Recon required first (runner, location convention, mocking pattern). No source-file changes; if a target is genuinely untestable without a refactor, stop and flag.

**Recon findings (presented in plan to Tony for confirmation):**
- Runner: Jest with `ts-jest` preset, default `jsdom` env, path alias `@/` → `<rootDir>/src/$1`, setup loads `@testing-library/jest-dom` + dotenv from `.env.local`
- Location convention: ALL tests in `tests/` at repo root. Sub-folders mirror source tree: `tests/lib/`, `tests/store/`, `tests/api/`, `tests/components/`, `tests/utils/`. NO co-located tests, NO `tests/services/` subdir yet.
- Naming convention: `<source-file-name>.test.ts(x)`
- Mocking pattern (gold standard from `tests/api/get-coupon-by-code.test.ts` and `products-by-category.test.ts`): `jest.mock("@/constants/apiEndpoints", () => ({...}))` to pin known URLs, then in describe scope save `originalFetch = global.fetch` and use `beforeEach/afterEach` to install/restore `global.fetch = jest.fn()`. Helper functions `mockOkResponse(body)` and `mockNotOkResponse(status)` build `as unknown as Response` shapes.
- Test idiom: `describe / test` (not `it`); AAA structure with blank lines; JSDoc header block at top describing what's tested + mocking strategy.

**Search-filter testability gotcha:** the predicate `d.name.toLowerCase().includes(query.toLowerCase())` is inline in `DealerList.tsx` — NOT importable as a pure function. Two options being presented to Tony in the plan: (A) component test via React Testing Library — render `<DealerList dealers={fixture} />`, simulate typing, assert visible cards. No source change. (B) Tiny refactor in `DealerList.tsx` to extract a named predicate. Source change, requires Tony's approval. Recommending A.

**Status:** Approved → executed → three test files written.

**Tony's additions to the plan (both applied):**
1. Verify `@testing-library/react` + `@testing-library/user-event` are in `package.json` BEFORE writing component tests — if missing, stop and report (do not install). Verified: `^16.3.0` + `^14.6.1` present (alongside `@testing-library/jest-dom ^6.6.3`). Gate passed.
2. Add a service test asserting fetch is called with `next: { revalidate: 30 }`. Added as the third test in the service suite: pulls `(global.fetch as jest.Mock).mock.calls[0][1]` and asserts `expect.objectContaining({ next: { revalidate: 30 } })`.

**Done:**
- `tests/services/dealerServices.test.ts` — NEW (~180 lines, 10 tests). Mocks `@/constants/apiEndpoints` (both endpoint constants to avoid module-load noise) + `global.fetch`. Helper functions `mockOkResponse`/`mockNotOkResponse` match the API-route-test gold standard from `tests/api/get-coupon-by-code.test.ts`. Tests: WP→Dealer mapping, `&_fields=acf` trim, `revalidate: 30` ISR option, not-ok response, fetch-throws, empty data array, missing `acf`, missing `dealer_data`, `dealer_data` not an array, missing-sub-field defaulting to `""`. NEW SUBDIR `tests/services/` created (mirrors `src/services/` per the existing mirror-source-tree convention).
- `tests/components/dealer-locator/DealerList.test.tsx` — NEW (~115 lines, 6 tests). Renders the real client component with a 4-row Dealer fixture and drives the input with `@testing-library/user-event`. Uses semantic queries (`getByPlaceholderText`, `getByRole`) to avoid brittle text-matching. Tests: empty query → all 4 visible, case-insensitive substring on "lake" → 2 matched, partial-substring on "ock" → 2 matched, uppercase "LAKE" → still 2 matched, "zzz" → empty-state line visible + zero cards, type-then-clear → 4 cards restored. NEW SUBDIR `tests/components/dealer-locator/` created (matches the existing `tests/components/cart/`, `tests/components/checkout/` feature-subdir pattern).
- `tests/lib/seoUtils.test.ts` — NEW (~155 lines, 11 tests). Pure synchronous function — NO mocks. Tests: string `@type === "Organization"`, array `@type` includes "Organization" (CRITICAL silent-failure case), array order doesn't matter, no Organization node → schema unchanged, existing `foundingDate`/`founder` preserved, null schema → null (no throw), non-object schema → input returned, missing `@graph` → unchanged, non-array `@graph` → unchanged, two Organization nodes → only first injected (locks in `break` behavior).

**Search-filter testability resolution:** Path A taken (component-render tests). `DealerList.tsx` NOT touched.

**Files NOT touched:**
- `src/services/dealerServices.ts`, `src/app/(public)/dealer-locator/DealerList.tsx`, `src/lib/seoUtils.ts` — source under test, untouched
- `jest.config.js`, `jest.setup.js`, `package.json` — no test-config or dep changes needed
- All other src/ and tests/ files — bytes-identical

**Files changed:**
- `tests/services/dealerServices.test.ts` — NEW (10 tests, ~180 lines)
- `tests/components/dealer-locator/DealerList.test.tsx` — NEW (6 tests, ~115 lines)
- `tests/lib/seoUtils.test.ts` — NEW (11 tests, ~155 lines)

**Total:** 27 new tests across 3 files. NOT executed; awaiting Tony's `npm test` run.

**Not committed** per Tony's instructions.

- **`acf` key in GET response ≠ write support.** The native WP REST `acf` integration enables READS but does not reliably handle repeater WRITES. On dockbloxx.mystagingwebsite.com specifically, the `POST /wp/v2/pages/{id}` with `{acf:{dealer_data:[...]}}` was silently accepted (200 OK) and silently no-op'd (no rows written, `_acf_changed: false`). For any future script-driven ACF seed against this install, confirm the `/wp-json/acf/v3/...` routes exist first (i.e. ACF to REST API plugin installed) before designing the request shape — or fall back to WP-CLI.
- **Attribute observed data carefully.** When the initial GET showed dealer 1 byte-perfect after the script ran, I leapt to "partial write succeeded" — but Tony had placed that exact row manually as a test before running the script. With only the post-run GET to look at, both hypotheses (partial write vs. pre-existing test row) fit equally; I should have asked Tony to confirm whether the pre-run page state was empty before diagnosing. Lesson: when the failure mode is "silent no-op," establishing the pre-state is required, not optional.

## End of Session State

- **Working:** All previously working systems unchanged. Dealer-locator WP page now has all 15 dealers populated (entered manually by Tony, not via script).
- **Broken:** Nothing. The script is on disk but inert for its current target; not committed.
- **Security posture:** Unchanged. 0 vulnerabilities in production.
- **Working tree:** Dirty (pre-existing carryover from May plus today's two new files). Modified set unchanged from session open except for the two new files (`scripts/seed-dealers.mjs`, `session_2026-06-11.md`) and the touched `RECOVERY.md`.
- **Next step:** Awaiting Tony's next task.

## Files Changed This Session

- `session_2026-06-11.md` — **NEW** (this file)
- `scripts/seed-dealers.mjs` — **NEW** (one-shot WP ACF dealer seeder; ran 200 OK but write was silent no-op for repeater — Tony populated rows manually instead)
- `src/constants/apiEndpoints.ts` — +1 line: new `DEALER_LOCATOR_REST_URL` export composed from `WP_REST_PAGES`
- `src/types/dealer.ts` — **NEW** (6 lines): `Dealer` interface (app-side clean shape, four string fields)
- `src/services/dealerServices.ts` — appended `getDealers()` for the dealer-locator feature; imports refactored; dealer-coupon functions untouched
- `src/app/(public)/dealer-locator/DealerLocatorContent.tsx` — swapped static `./data` import for `getDealers()` service call; made function async; markup unchanged. Then, in the search task: removed react-icons imports (cards moved out), added `DealerList` import, replaced grid block with `<DealerList dealers={dealers} />`.
- `src/app/(public)/dealer-locator/DealerList.tsx` — **NEW** client component owning search state + filtered card grid + empty state
- `src/lib/seoUtils.ts` — appended `injectOrganizationFacts(schema)` + private `isOrganizationNode(node)` helper for homepage JSON-LD enrichment
- `src/app/(public)/page.tsx` — wired `injectOrganizationFacts` into the schema pipeline (object → inject → stringify → fixUrl); homepage only
- `src/app/(public)/dealer-locator/data.js` — **DELETED** (static fallback retired now that live-data path is verified; `tsc --noEmit` clean)
- `tests/services/dealerServices.test.ts` — **NEW** (10 unit tests for `getDealers`)
- `tests/components/dealer-locator/DealerList.test.tsx` — **NEW** (6 component tests for the search filter)
- `tests/lib/seoUtils.test.ts` — **NEW** (11 unit tests for `injectOrganizationFacts`)
- `RECOVERY.md` — refreshed pointer to current state
