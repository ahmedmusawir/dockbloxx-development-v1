# Session Log: 2026-07-08

## Project Context
- **Project:** Dockbloxx (headless Next.js + WooCommerce storefront)
- **Tool:** Claude Code
- **Goal:** TBD — session file opened at start of day per CLAUDE.md mandatory protocol. Tony's opener signals **Ticket 2** work is next (probably WooCommerce attribution / UTM capture — the pipe explicitly deferred out of Ticket 1 scope). Concrete task will be captured here once Tony states it.

## Starting State
- **Branch:** `ticket-2-utm-to-rest` (Tony created + switched me to it). Branch name confirms the T2 theme: **UTM to REST** — writing attribution data to the WooCommerce REST API. Matches Ticket 1 ORIENTATION.md's "Pipe 2" explicit deferral.
- **HEAD:** `f4def81 Ticket 1 done` — same as `main`. Fresh T2 branch off the merged-main tip.
- **Backup branch:** `main-before-ticket-1` exists (Tony's safety practice). Nice.
- **Working tree:** Clean.
- **Ticket 1: CLOSED.** Tony's message: "T1 tested and merged with main." Commits `8d89b74` (Track A app fix) + `f4def81` (Ticket 1 done) both on main. T1 Track A code confirmed intact in the merged tip (grep still hits `ga4_purchase_fired` + `localStorage.removeItem` in `ThankyouPageContent.tsx`). Track B GTM changes + Phase 2 validation must have been completed by Tony + Coach between last session close (2026-07-02) and this session open (2026-07-08).
- **Ticket 1 A3 candidate:** moved by Tony from the CURRENT_TASKS root into `Ticket_1_Solution_Module/Ticket_1_A3_followup_candidate.md` — consolidated with the rest of the T1 module post-close.
- **Folder restructure:** Tony renamed `Ticket_1_Solution_Pack` → `Ticket_1_Solution_Module` and added a top-level `ReadMe.md` at `agent_docs/CURRENT_TASKS/ReadMe.md` (one-line: "SM or Solution modules reside for Dockbloxx related Clickup Tickets"). Standard structure going forward: `TicketX_Solution_Module/{CLAUDE.md, GUARDRAILS.md, README.md, EVIDENCE_ADDENDUM.md?, references/, templates/, workflow/, examples/}`.
- **Ticket 2 pack:** dropped at `agent_docs/CURRENT_TASKS/Ticket_2_Solution_Module/`. Structure mirrors T1 exactly (CLAUDE.md 7006 bytes, GUARDRAILS.md 3022, README.md 2267, plus references/templates/workflow/examples subfolders). No EVIDENCE_ADDENDUM.md yet — that would only appear after a live investigation like T1's mid-arc pivot.
- **NOT yet read:** T2 pack manager. Per T1 pack's activation protocol, reading `Ticket_2_Solution_Module/CLAUDE.md` is what kicks off the T2 arc. Holding until Tony explicitly points me at it.
- **Security posture:** 0 vulnerabilities in production. Unchanged.

## Context Carried Forward From 2026-07-01/02 (session_2026-07-01.md)

### Ticket 1 Track A summary (already in commit `8d89b74`)
- **A1** applied: `hasTrackedPurchase` useRef guard removed. Order-ID-keyed sessionStorage lock in `src/app/(public)/thankyou/ThankyouPageContent.tsx`. Key: `` `ga4_purchase_fired_${parsed.id}` ``, value `"1"`. Survives refresh + back-nav within tab. Per-order (GUARDRAIL 7).
- **A2** applied: `localStorage.removeItem("latestOrder")` inside guarded fire block after `trackPurchase(parsed)`. Kills the cross-tab landmine.
- **A3** report-only handoff written (`agent_docs/CURRENT_TASKS/Ticket_1_A3_followup_candidate.md`). Same-pattern `isCustomerRegistered` remount bug documented. Low severity (no revenue impact — WP protected by email uniqueness; GA4 `user_signup` count inflation only).
- `npx tsc --noEmit` was clean after each edit.

### Ticket 1 pack v1.1 documents (all authored by Tony overnight 2026-07-02, in commit `8d89b74`)
- `Ticket_1_Solution_Pack/EVIDENCE_ADDENDUM.md` — the corrected two-track root cause. E1-E10 evidence log. Ownership split.
- `Ticket_1_Solution_Pack/GUARDRAILS.md` v1.1 — rules 9-11 revised/new. Notably rule 10 (Claudy never touches or compensates for GTM) and rule 11 (adjacent findings report-only).
- `Ticket_1_Solution_Pack/workflow/01_SOLUTION.md` v1.1 — Track A / Track B rewrite.
- `Ticket_1_Solution_Pack/README.md` v1.1.
- `Ticket_1_Solution_Pack/CLAUDE.md` v1.1 (patched — I applied the four edits from `CLAUDE_MD_PATCH.md`, then deleted the patch file per Tony).

### Ticket 1 Track B (GTM container — Tony + Coach, outside repo)
- **B1** — remap `transaction_id` in "ga4 - event settings" variable OFF the generated `event_id` (E6 smoking gun: app pushed `14875` → GA4 received `transaction_id: "1782960176956_17829608237663"`). Preflight with Coach on GAP-1 (was the `event_id` mapping intentional for a Stape-era client/server dedup design?).
- **B2** — retire `server_container_url = "https://fp.dockbloxx.com"` from "ga4 - config settings" (retired Stape relay, GAP-2). Separate workspace/version after B1.
- **Ticket 1 CLOSES ONLY** when both tracks done + Phase 2 DebugView on property `443304844` shows one purchase per order with real Woo order ID.

### Bookkeeping questions RESOLVED at session start
- ~~T1 Track B publish status~~ → T1 CLOSED, merged to main.
- ~~T2 branch strategy~~ → `ticket-2-utm-to-rest` created by Tony off main tip `f4def81 Ticket 1 done`.
- ~~T1/T2 concurrency~~ → T1 done, T2 is the sole active arc.

## Session Progress

### [08:50] — IN PROGRESS — T2 activation (playback pending Tony's OK)
**Read (in the order the T2 manager prescribes):** `Ticket_2_Solution_Module/CLAUDE.md` (v1.0), `GUARDRAILS.md` (10 rules), `README.md` (operator-facing), `references/ORIENTATION.md` (two-pipes model — T1 = GA4 pipe DONE; T2 = WC attribution pipe THIS ticket), `workflow/00_RECON.md` (Phase 0 questions A-H), plus both templates for shape (`RECON_FINDINGS.md`, `CONTRACT.md`).
**Understanding (three-point playback for Tony):**
1. **DoD** = REST API readback only. WP admin Orders-screen display is EXPLICITLY OUT (known WP wall per Tony's operator decision + GUARDRAIL 7 + manager § 2).
2. **Scope** = WooCommerce order-attribution pipe only. No GA4, no GTM, no analytics events. GA4 enrichment with UTMs is a possible future ticket, never this DoD. T1's pipe is closed and out of scope here.
3. **Capture is currently gone.** Coach's WP-footer capture script was pulled because it raced the E2E suite. Reader (`src/lib/attribution.ts` per manager hypothesis) still exists but reads EMPTY. My Phase 0 job = map the exact key contract across every layer (URL param → storage key → reader key → Woo meta key → REST readback field) BEFORE any code, filling `CONTRACT.md` as the locked map. Key mismatch is the silent-empty-attribution trap.
**Task list refreshed:** 9 Phase 0 tasks created (#11-#19: A-H recon + fill templates). Old T1 tasks (#8/#9/#10) stay `completed` as history.
**Status:** Presenting playback in CLI. Not yet touching the codebase. Awaiting Tony's OK on the three-point playback before entering Phase 0 read-only recon.

### [09:05] — APPROVED → COMPLETE — Phase 0 Recon (read-only)
**Approved at:** 08:55-ish ("Go — run Phase 0 recon. Read-only, Plan Mode, zero edits.")
**Also noted (Tony):** CLAUDE.md at repo root updated to v3.1 with new RESPONSE LOGGING PROTOCOL — I read the full updated file. Key change: substantive artifacts (plans, reports, investigation results) MUST be written to `agent_docs/RESPONSES/response_<YYYY-MM-DD>_<HHMMSS>_<slug>.md` BEFORE printing to screen. Session file tracks status transitions; RESPONSES/ holds the standalone readable artifact. Both writes fire.
**Files read:** `src/lib/attribution.ts` (49 lines — reader intact); `src/components/checkout/payments/StripePaymentForm.tsx:60-100` (consumer — attaches attribution to orderPayload); `src/components/checkout/right-pane/ApplyCoupon.tsx:1-50` (second reader consumer — QR-code coupon feature); `src/lib/orderTransform.ts` (208 lines — forward point, hybrid coach-prefixed + partial buggy native); `src/app/api/place-order/route.ts` (proxy — no attribution logic); `src/app/layout.tsx:40-95` (Script tag for coach-attribution is present but empty payload); `docs/wp-plugins/attribution-script.md` (199-line archived Coach script — capture logic + E2E race cause); `docs/wp-plugins/cyberize-attribution.php:1-100` (WP plugin metadata, unused in prod per CLEANUP_BACKLOG); `docs/ghl-attribution/FRONTEND-CONTRACT.md` (stale doc with `dbx_` prefixes that never made it live); `docs/ghl-attribution/GHL_ATTRIBUTION_SESSION.md` (key section — confirms `_cltk` mention); `agent_docs/CLEANUP_BACKLOG.md` (attribution + GHL notes); `agent_docs/TESTING_PLAYBOOK.md:1520-1560` (G11 pattern — third-party root-layout script race root cause).

**Key findings (four headlines):**
1. **Reader intact.** sessionStorage-based. 9 fields under un-prefixed keys — BUT gclid read from `_cltk` (silent-failure trap). Two consumers: StripePaymentForm (primary) + ApplyCoupon (QR-code adjacent).
2. **Forward: HYBRID (coach-prefixed + partial buggy native).** Every attribution field written as `_coach_ghl_<field>`; ONE native `_wc_order_attribution_source_type` with wrong value (stores utm_source string instead of WC enum). Other 6 native keys not written. gclid/fbclid only as `_coach_ghl_*`.
3. **Capture confirmed gone.** Script tag exists but WP ACF field is empty. No React provider. Every order gets `_coach_ghl_attribution_captured_at: <ts>` + `_wc_order_attribution_source_type: "direct"`. Matches T2 problem statement.
4. **CONTRACT.md locked (proposal).** 10-row 5-column table. Storage type = sessionStorage. Columns 2↔3 identical strings per row. Reader needs 4 one-line edits in Phase 1 (drop `utm_keyword` fallback, `_cltk` → `gclid`, add source_type + referrer rows, rename interface field).

**Open GAPs marked (not guessed):**
- GAP-1: staging URL (`dbp.dockbloxx.com` vs `dockbloxx.mystagingwebsite.com`).
- GAP-2: `source_type` accepted WC enum values.
- GAP-3: gclid history (`_cltk` vs `gclid` — informational).
- GAP-4: any downstream consumer of `_coach_ghl_*` still active?

**Adjacent findings (report only per GUARDRAIL 10):**
- Buggy `_wc_order_attribution_source_type` value logic (utm_source string, not enum) — latent bug even without capture.
- `<Script id="coach-attribution">` in layout.tsx is a no-op — Phase 1 cleanup candidate (GUARDRAIL 9 gate).
- `src/app/layout-org.tsx` backup file, unused, out of T2 scope.

**Files written this phase:**
- `agent_docs/CURRENT_TASKS/Ticket_2_Solution_Module/templates/RECON_FINDINGS.md` (filled, ~230 lines).
- `agent_docs/CURRENT_TASKS/Ticket_2_Solution_Module/templates/CONTRACT.md` (filled — locked 10-row 5-column table + first-touch invariant + Phase 2 validation checklist).
- `agent_docs/RESPONSES/response_2026-07-08_091452_t2-phase0-recon.md` (RESPONSE LOGGING PROTOCOL mirror of on-screen output — per updated CLAUDE.md v3.1).
- **Zero source-code changes.**

**Status:** STOPPED per T2 workflow Stop Gate. Awaiting Tony's approval on BOTH templates before Phase 1. Awaiting answers to the four GAPs (or explicit "proceed with proposed answers") to fully lock CONTRACT.md.

### [09:35] — APPROVED (recon + CONTRACT accuracy) — HOLD before Phase 1 pending Coach on source_type
**Tony's approval (verbatim):** "Recon and CONTRACT approved as accurate — good work, especially the _cltk catch and flagging the source_type bug. But do NOT enter Phase 1 yet. One design question (native source_type enum vs Coach's descriptive values) has to be answered by Coach before we lock the write mapping, because our DoD is REST readback and the field shape depends on how Coach reads it."

**Five decisions LOCKED for Phase 1 (fold into CONTRACT + Solution when we resume):**
1. **Staging / test backend = `dockbloxx.mystagingwebsite.com`** (dev/staging). **`dbp.dockbloxx.com` is PRODUCTION — do NOT test against it.** Closes GAP-1. `NEXT_PUBLIC_BACKEND_URL` in `.env.local.example` was correct all along; the T2 manager § 3 reference to `dbp.dockbloxx.com` was for context on the PROD backend, not the Phase 2 test surface.
2. **Remove all `_coach_ghl_*` writes.** We're off the GHL route entirely. Closes GAP-4 with explicit go-ahead per GUARDRAIL 9. Phase 1 Solution deletes the `Object.entries(attribution).map(([key, value]) => ({ key: \`_coach_ghl_${key}\`, value }))` block at `src/lib/orderTransform.ts:170-174`.
3. **gclid canonical key = `gclid`** (not `_cltk`). Reader's `_cltk` line was always broken (GAP-3 informational answer confirmed). Phase 1 reader edit at `src/lib/attribution.ts:34`: `_cltk` → `gclid`.
4. **`utm_term` is canonical** (not `utm_keyword`). The `utm_term → utm_keyword` mapping in Coach's script was a GHL form-field-population quirk, dropped. Phase 1 reader edit at `src/lib/attribution.ts:33`: drop the `utm_keyword` fallback; rename interface field at line 11. CONTRACT.md row 5 already reflects this.
5. **Delete the empty `<Script id="coach-attribution">` in `src/app/layout.tsx:85-89`.** No-op today (empty ACF payload). Closes the GUARDRAIL 9 gate — approved for removal in Phase 1.

**Still open (BLOCKS Phase 1 Solution locking):**
- **Coach's answer on the `source_type` design question (GAP-2 unresolved).** Tony framed this: the choice between (a) native WC enum values (`utm | referral | organic | typein | admin | mobile_app`) vs (b) Coach's descriptive values ("google", "facebook", etc.) — depends on how Coach's downstream reads `source_type` via REST. Since our DoD IS the REST readback, the field's value SHAPE matters. Cannot lock the classifier logic in the new capture provider until this is answered.

**Coach question in one line:** "When your team reads `_wc_order_attribution_source_type` off the WC REST order response, do you expect a WC enum (utm/referral/organic/typein) or descriptive strings (google/facebook/ai-referral)?"

**What I do NOT touch while standing by:** `CONTRACT.md`, `RECON_FINDINGS.md`, `src/`, any Phase 1 planning artifacts. Session file + RECOVERY.md are the ONLY writes I do while on hold — they're disaster-recovery bookkeeping, not content edits.

**Status:** STOOD DOWN. Standing by for Coach's source_type answer + Tony's "resume" signal.
**Trigger:** Tony's "new day ... start a new session file ... get ready for ticket #2 instructions" (08:37 area), followed by "T1 tested and merged with main ... new branch for T2 created" (08:41).
**Verified:** on `ticket-2-utm-to-rest`, clean tree, T1 code intact in merged main tip. Discovered folder restructure (Pack → Module rename, top-level `ReadMe.md`, `Ticket_2_Solution_Module/` landed with mirror structure). Read the top-level ReadMe (86 bytes, index only). Listed T2 module structure without opening the T2 manager — that's Tony's call to trigger.
**Not yet done:** T2 pack read. T2 recon. Any T2 work.
**Awaiting:** explicit instruction to read `Ticket_2_Solution_Module/CLAUDE.md` (or equivalent), which per T1 pack precedent kicks off the T2 arc.

## Lessons Learned

_(To be filled in as the session progresses.)_

## End of Session State

_(To be filled in at session close.)_

## Files Changed This Session

_(To be filled in as changes are made.)_
