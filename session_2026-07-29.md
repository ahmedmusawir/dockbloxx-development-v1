# Session Log: 2026-07-29

## Project Context
- **Project:** Dockbloxx (headless Next.js + WooCommerce storefront)
- **Tool:** Claude Code
- **Goal:** Coach's detailed answers to the T2 `source_type` design question have landed. Tony + team are analyzing them. Session opened to be ready when the analysis produces a decision → then fold-in + Phase 1 kickoff. Concrete task captured here once Tony states it.

## Starting State
- **Branch:** `ticket-2-utm-to-rest` (unchanged since 2026-07-08).
- **HEAD:** `f4def81 Ticket 1 done` (unchanged — no new commits overnight).
- **Working tree carryover (all uncommitted per standing rule):**
  - `CLAUDE.md` — v3.1 RESPONSE LOGGING PROTOCOL update (Tony)
  - `RECOVERY.md` — refreshed 2026-07-28 for recap; will refresh again this session
  - `agent_docs/CURRENT_TASKS/Ticket_2_Solution_Module/templates/CONTRACT.md` — Phase 0 locked proposal
  - `agent_docs/CURRENT_TASKS/Ticket_2_Solution_Module/templates/RECON_FINDINGS.md` — filled ~230 lines
  - `session_2026-07-08.md` — Phase 0 arc + five locked decisions at `[09:35]`
  - Untracked: `agent_docs/RESPONSES/` (contains Phase 0 recon mirror + yesterday's recap report)
  - Untracked: `session_2026-07-28.md` (yesterday's recap session)
- **Zero `src/` edits** over the entire T2 arc.
- **Active ticket:** **Ticket 2 — capture UTM/click IDs → WooCommerce order meta.**
- **Phase 0 status:** COMPLETE + APPROVED for accuracy (2026-07-08 [09:35]).
- **Phase 1 status:** STILL STOOD DOWN — Coach's answer has arrived per Tony, but Tony + team are analyzing before locking direction.

## Context Carried Forward From 2026-07-08 (still authoritative)

### Five decisions Tony locked on 2026-07-08 (unchanged, awaiting fold-in to CONTRACT.md)
1. **Staging = `dockbloxx.mystagingwebsite.com`.** `dbp.dockbloxx.com` = PRODUCTION, off-limits for Phase 2 testing.
2. **Delete all `_coach_ghl_*` writes** from `src/lib/orderTransform.ts:170-174`.
3. **gclid canonical key = `gclid`** (not `_cltk`).
4. **`utm_term` canonical** (drop `utm_keyword` fallback + rename interface field).
5. **Delete empty `<Script id="coach-attribution">`** from `src/app/layout.tsx:85-89`.

### The one open blocker (about to resolve)
- **GAP-2 · `source_type` field-shape design** — Coach has responded (per Tony this session). Two options previously sketched:
  - **Enum path:** `source_type = utm` when UTMs present, `organic` when search referrer, `referral` when external non-search, `typein` when no UTMs + no referrer.
  - **Descriptive path:** reuse Coach's classification from archived script — AI, Social, Search, Referral, Direct with domain-specific strings (`google`, `facebook`, `ai-referral`, etc.).
- Whichever Coach chose determines the classifier logic in the new capture provider.

## Session Progress

### [09:50] — COMPLETE — Session-start bookkeeping
**Tony's opener:** "good morning jarvis ... Tony Stark here ... great job on the report ... it's a new day ... so let's start by making a new session file for today ... then wait for your new task ... good news is coach sent detailed answers ... we're analyzing those now ..."
**Disk check:** confirmed nothing changed overnight — same 5 modified + 2 untracked, same HEAD. No new commits.
**Created:** this session file (`session_2026-07-29.md`).
**Updated:** `RECOVERY.md`'s "Last touched" pointer + active-session pointer.
**Status:** Standing by. Not touching CONTRACT.md, RECON_FINDINGS.md, or `src/` until Tony delivers Coach's answer + green-light. When it lands, expected first action = fold-in to CONTRACT.md, then Phase 1 kickoff per `workflow/01_SOLUTION.md`.

### [11:16] — PENDING_APPROVAL — Fold Coach's answers into CONTRACT.md (locked-proposal → locked-final)
**Task:** Tony green-lit Phase 1 resume and delivered Coach's 7-item answer set. Fold all 7
into `templates/CONTRACT.md` in one Write, becoming LOCKED-FINAL. Diff shown, stop for approval
before Phase 1 plan.
**Coach's resolutions folded:** (1) GAP-2 = ENUM `{utm,organic,referral,typein}`, never blank,
Row 6 table locked; (2) GAP-3 closed informational, gclid canonical; (3) SCOPE DELTA wp-admin
visibility now in DoD (Origin + Source type on native box; early render check → escalate);
(4) SCOPE DELTA four click IDs — added wbraid+gbraid, recommend bare naming `gclid/fbclid/
wbraid/gbraid`; (5) SCOPE DELTA CAT params fallback twins of utm (utm wins), resolved at
capture, cat_source alone → source_type=utm, escape-hatch assessed in Phase 1 plan; (6) test
URLs → Phase 2 canonical cases; (7) five 2026-07-08 decisions + KEEP landing_page / REMOVE
attribution_captured_at.
**Artifacts:** proposed content staged at scratchpad/CONTRACT_proposed.md; response mirror at
`agent_docs/RESPONSES/response_2026-07-29_111637_t2-contract-foldin.md`.
**Status:** Awaiting approval. NO live-file write yet. Zero src/ edits.

### [11:17] — COMPLETE — CONTRACT.md is LOCKED-FINAL
**Task:** Fold Coach's 7-item answer set into CONTRACT.md.
**Approved at:** 11:17 (Tony: "approved, bare keys are good — save it"). Bare click-ID naming
(`gclid/fbclid/wbraid/gbraid`) confirmed. CAT-resolves-at-capture design accepted (no pushback).
**Files changed:** `templates/CONTRACT.md` (161 → ~280 lines; byte-verified == approved diff).
**Tests:** N/A (doc-only). `diff -q` confirmed live file == approved content.
**Notes:** Still zero `src/` edits, no commits. Next = Phase 1 plan in Plan Mode per
`workflow/01_SOLUTION.md`, presented before any src/ change. One item for Tony to relay to
Coach downstream (non-blocking): final bare click-ID names locked.

### [11:20] — PENDING_APPROVAL — Phase 1 plan (T2 implementation)
**Task:** Implement the fix per `workflow/01_SOLUTION.md`, one change at a time, checkpoint each.
Plan below awaiting approval; NO src/ edits yet.

**CAT escape-hatch assessment (item 5):** utm-vs-cat resolution = per-field `utm_x || cat_x`,
~5 lines in the provider. SMALL ADD → recommend SHIP TOGETHER (not deferred to its own task).

**Step 1 — Capture provider (NEW file, `"use client"`).** Location TBD (propose
`src/components/providers/AttributionProvider.tsx`). Mounted in `layout.tsx` wrapping `{children}`.
`useEffect` on mount only. Reads `window.location.search` + `document.referrer` +
`window.location.pathname`. Per-field utm-vs-cat resolve (utm wins). Classify `source_type`
4-way enum. First-touch persist (write key only if absent) for: utm_source, utm_medium,
utm_campaign, utm_content, utm_term, source_type, referrer, gclid, fbclid, wbraid, gbraid,
landing_page, AND coupon (keeps ApplyCoupon QR autofill working). NO DOM side effects
(no replaceState / href rewrite / form injection / iframe patch / setInterval — GUARDRAIL 5).
ALSO in this step: delete `<Script id="coach-attribution">` (layout.tsx:85-89) [decision 5];
dead-code consequence → `footerJS` (line 52) + `footer` destructure (line 48) orphaned →
propose removing, ask first.
**OPEN DECISION 1a (classification scope):** Option A = capture only explicit utm_*/cat_* +
click IDs + referrer + landing (organic/referral orders get source_type but blank utm_source);
Option B = also synthesize utm_source (referrer domain) + utm_medium (organic/referral/social/
ai-referral) from referrer when no UTMs, Coach-parity → richer WC Origin. Need Tony's pick.
**OPEN DECISION 1b:** provider file location.

**Step 2 — Reader reconciliation (`src/lib/attribution.ts`).** line 33 utm_keyword→utm_term;
line 34 `_cltk`→`gclid`; add source_type/referrer/wbraid/gbraid rows; interface (line 11) rename
utm_keyword→utm_term + add 4 fields; REMOVE attribution_captured_at (line 16 + line 38). Keep
coupon + landing_page. Grep-confirmed safe: no external consumer references changed fields.

**Step 3 — Forward native meta (`src/lib/orderTransform.ts:168-181`).** Delete `_coach_ghl_*`
map block (170-174) [decision 2]. Build native meta explicitly, each only if present:
7× `_wc_order_attribution_{utm_source,utm_medium,utm_campaign,utm_content,utm_term,source_type,
referrer}` + 4× top-level click IDs (gclid/fbclid/wbraid/gbraid) + landing_page. EXCLUDE coupon
from forward. Fix source_type value → read `attribution.source_type` verbatim (kill the
`utm_source || "direct"` bug at line 178). METADATA ONLY (GUARDRAIL 2).

**Step 4 — Direct-traffic fallback.** Resolved AT CAPTURE: source_type always classified
(min `typein`), so forward writes verbatim — no `|| direct` needed. Enum has no `direct` member;
direct traffic = `typein`. Confirm this fallback contract with Tony.

**Files:** CREATE provider (+ optional Jest unit test for pure classify/resolve fns); MODIFY
layout.tsx, attribution.ts, orderTransform.ts. NOT touched: StripePaymentForm.tsx, ApplyCoupon.tsx,
checkout billing/shipping/line-items/coupon logic.
**Status:** Awaiting approval. Zero src/ edits.

### [12:51] — APPROVED → IN PROGRESS — Phase 1 execution
**Approved at:** 12:51 (Tony: "APPROVED — proceed", after external review of the full plan text).
Decisions locked: Coach-parity classification; provider at `src/components/providers/`; CAT ships
together; Jest unit test yes; bare click-ID key names (`gclid`/`fbclid`/`wbraid`/`gbraid`) — relay
to Coach PENDING until after Phase 2.
**Plan mirror:** `agent_docs/RESPONSES/response_2026-07-29_125058_t2-phase1-plan.md` (verbatim).
**Execution rules (restated by Tony):** one change at a time; diff before saving; checkpoint after
EACH step, STOP and wait for approval before the next; no commits/pushes; no GA4/GTM; first-touch
only; adjacent findings report-only.

**Two external-review flags to carry (neither changes the plan):**
- **FLAG 1 — direct-traffic rendering.** `typein` + `utm_source='direct'` + `utm_medium='(none)'`
  may render oddly in the WC Origin box (e.g. "Direct: direct" or ignored utm_source). Do NOT
  change capture logic. Added to CONTRACT.md Phase 2 wp-admin checklist as its own line:
  "verify Origin rendering for typein orders; screenshot; report to Coach."
- **FLAG 2 — dropped utm_campaign placeholder.** Approved to NOT port Coach's '(organic)/(referral)'
  campaign placeholder. Added to EVIDENCE_LOG.md as a Coach-relay item.

**Step 1 checkpoint expectations (Tony):** show diffs for all three artifacts
(attributionCapture.ts, AttributionProvider.tsx, layout.tsx) BEFORE saving any; include a
per-bucket domain table (AI/social/search) for eyeball parity vs `docs/wp-plugins/attribution-script.md:6-41`;
confirm twitter-normalization (x.com/t.co → twitter) carried over.
**HARD STOP** was requested earlier after mirror write; superseded by this APPROVED → proceed to
begin Step 1 (present diffs, no save yet).

### [12:58] — COMPLETE — Step 1 (capture provider + retire old script)
**Approved & saved.** Files:
- CREATE `src/lib/attributionCapture.ts` (pure logic: classifyReferrer/resolveUtmCat/deriveSourceType/buildAttribution)
- CREATE `src/components/providers/AttributionProvider.tsx` (`"use client"`, effect-only, first-touch, no DOM side effects)
- CREATE `tests/lib/attributionCapture.test.ts` (23 tests incl. bing→organic, internal→null, Case A/B shape)
- MODIFY `src/app/layout.tsx` (mount provider, delete coach-attribution Script, drop dead footer/footerJS)
- DOC: EVIDENCE_LOG note (classifyReferrer verbatim port caveat); CLEANUP_BACKLOG (/types move deferred)
**Tests:** `npx jest tests/lib/attributionCapture.test.ts tests/api/place-order.test.ts` → **35 passed / 0 failed**
(23 new + 12 existing place-order intact). Ironman Rule held.
**Operator notes honored:** provider position as proposed; WcSourceType stays local (→ CLEANUP_BACKLOG);
classifyReferrer verbatim caveat logged; test suite includes bing→organic, internal→null, Case A/B asserts.
**Checkpoint:** STOPPED for Tony's approval before Step 2 (reader reconciliation).

### [13:10] — Session calibration (Tony, standing for rest of session)
STOP-for-approval = `src/` changes ONLY (the 3 step checkpoints). All `agent_docs/` bookkeeping
(session, RECOVERY, EVIDENCE_LOG, RESPONSES mirrors) = pre-approved standing work → write silently
inline, no pause/confirm. Stops only at end of Step 1 / 2 / 3, each with diffs.

### [13:23] — COMPLETE — Step 2 (reader reconciliation)
**Saved:** `src/lib/attribution.ts` — interface `utm_keyword`→`utm_term`, +`source_type`/`referrer`/
`wbraid`/`gbraid`, −`attribution_captured_at`; `getAttribution()` reads matched to contract keys
(gclid now from `gclid` not `_cltk`); `cleanAttribution` untouched. Two "Coach's script" comments
refreshed to name AttributionProvider (flagged in-scope; revertable).
**Tests:** `npm test` → **216 passed / 216 total** (19 suites). `npx tsc --noEmit` → **0 errors**
project-wide (0 attribution-related). Rename cascade clean. Ironman Rule held.
**Mirror:** `agent_docs/RESPONSES/response_2026-07-29_132355_t2-step2-complete.md`
**Checkpoint:** STOPPED for Tony's approval before Step 3 (forward native meta in orderTransform.ts).

### [13:31] — COMPLETE — Step 3 (forward native Woo meta) + Step 4 (no-op)
**Saved:** `src/lib/orderTransform.ts` — added `buildAttributionMeta()` helper (+ WC_ATTRIBUTION_META
map, TOP_LEVEL_META_KEYS); replaced the attribution meta_data block (deleted `_coach_ghl_*` map AND
the `utm_source||"direct"` source_type bug). 7 native `_wc_order_attribution_*` + 4 bare click IDs +
landing_page, conditional (no empty writes), coupon excluded, source_type verbatim.
**Test added:** `tests/api/place-order.test.ts` — full (12 entries, coupon absent, source_type verbatim,
no coach keys) + sparse (2 entries) + no-attribution (empty) cases.
**Tests:** `npm test` → **219 passed / 219** (19 suites, +3 new). `npx tsc --noEmit` → **0 errors**.
**Step 4 (direct fallback): NO CODE** — resolved at capture (typein/direct/(none)), forward writes
verbatim. Phase 1 implementation FUNCTIONALLY COMPLETE.
**Mirror:** `agent_docs/RESPONSES/response_2026-07-29_133115_t2-step3-complete.md`
**Checkpoint:** STOPPED for Tony's review. Next = Phase 2 staging REST readback (`workflow/02_TESTING.md`).

### [15:41] — COMPLETE — Phase 2 Finding 1 fix (atomic first-touch)
**Trigger:** manual Phase 2 testing found chimera attribution across multi-landing sessions
(runs 14883/14884 invalid). Tony approved the src/ amendment.
**Saved:** `src/lib/attributionCapture.ts` (+`persistFirstTouch()` + `ATTRIBUTION_GUARD_KEY`
`attribution_captured`); `src/components/providers/AttributionProvider.tsx` (uses atomic persist);
`tests/lib/attributionCapture.test.ts` (+2 atomicity tests; also had to add persistFirstTouch to
the import — first run failed 2 tests / 3 tsc errors, fixed, re-green).
**Behavior:** first landing captures full snapshot + sets guard; later landings skip entirely
(no gap-filling). Guard-on-sparse-capture is correct first-touch (direct IS a first touch) —
per Tony, documented, not to be re-litigated. Guard key not read by getAttribution → never forwarded.
**Tests:** `npm test` → **221 passed / 221** (19 suites). `npx tsc --noEmit` → **0 errors**.
**Bookkeeping:** EVIDENCE_LOG — 14883/14884 marked SUPERSEDED + Finding 1 (fixed) + Finding 2
(report-only) logged; CLEANUP_BACKLOG — legacy tracker retire item added (do NOT fix in T2).
**Mirror:** `agent_docs/RESPONSES/response_2026-07-29_154105_t2-finding1-complete.md`
**Next:** Tony runs 3 clean cases (fresh incognito each) → hands order IDs → I do REST readback + fill EVIDENCE_LOG.

### [16:58] — Phase 2 REST readback — Case A + B PASS (core DoD proven)
**Readback (I ran via curl, staging):**
- **Order 14887 (Case A)** — 9 meta entries: 6 native `_wc_order_attribution_*` (source=facebook,
  medium=paid_social, campaign=summer_sale_2026, content=carousel_a, term=dock_bumper, source_type=utm)
  + gclid + fbclid + landing_page. **PASS.** referrer absent (empty document.referrer, correct no-empty-write).
- **Order 14888 (Case B)** — 7 meta entries: 4 native (source=google, medium=cpc, campaign=brand,
  source_type=utm) + gclid + wbraid + landing_page. **PASS.**
- **Both:** `attribution_captured` guard NOT forwarded (verified absent); zero `_coach_ghl_*`. Full
  1→5 CONTRACT trace holds. 14887 confirmed = facebook order (ID cross-checked via meta content).
- **typein (Case): PENDING order ID** from Tony → then readback + Test 5 (direct fallback).
**EVIDENCE_LOG:** filled (env, results table, Case A/B verbatim meta_data + assertions, CONTRACT
validation). Superseded list now 14883/14884/14886. Finding 2 addendum (_cltk live not residue) +
Finding 3 (warmup.html nav, report-only) logged in EVIDENCE_LOG + CLEANUP_BACKLOG.
**Mirror:** `agent_docs/RESPONSES/response_2026-07-29_165758_t2-phase2-readback.md`
**Next:** Tony does wp-admin visual checks (Origin + Source type render; typein FLAG 1) + gives typein order ID.

## Lessons Learned

- Multi-landing sessions break per-key first-touch (chimera records). Atomic snapshot + guard key
  is the correct pattern for session-scoped first-touch attribution.
- REST readback is doable from the agent sandbox (curl + creds, HTTP 200) — extract only `.meta_data`
  to keep PII out of logs. The browser-interactive parts (checkout, wp-admin) remain Tony's.

## End of Session State (closed 2026-07-29 ~17:00)

- **Working:**
  - Phase 1 implementation COMPLETE & accepted (Steps 1–4). Capture provider → reader → native forward all agree on CONTRACT keys.
  - Atomic first-touch (Finding 1 fix) in place — no chimera attribution across multi-landing sessions.
  - Full suite **221/221**, `tsc --noEmit` **0 errors**.
  - Phase 2 core DoD PROVEN for tagged traffic: REST readback **Case A (14887)** + **Case B (14888)** PASS, verbatim meta in EVIDENCE_LOG. Guard key not forwarded; zero `_coach_ghl_*`.
  - wp-admin visual checks for A & B: Tony reports both render & good (exact Origin/Source-type values + screenshots still to be attached to EVIDENCE_LOG/ClickUp).
- **Broken:** nothing.
- **Next steps (resume here):**
  1. **typein order** — place it, hand me the ID → I run readback + close Test 5 (expect source_type=typein, utm_source=direct, utm_medium=(none), no click IDs/campaign).
  2. Attach wp-admin screenshots + record Origin/Source-type values in EVIDENCE_LOG (incl. typein FLAG 1 rendering).
  3. Finalize EVIDENCE_LOG attachments checklist; confirm the two Coach relay items (dropped campaign placeholder; bare click-ID names).
  4. Tony's call on commit/PR (nothing committed yet — standing rule).
  5. Report-only follow-ups (NOT T2): legacy tracker retire (Finding 2 + addendum), warmup.html nav (Finding 3) — in CLEANUP_BACKLOG.

## Files Changed This Session

**src/ (Phase 1 implementation):**
- `src/lib/attributionCapture.ts` — **NEW** (pure capture logic + `persistFirstTouch` atomic guard)
- `src/components/providers/AttributionProvider.tsx` — **NEW** (`"use client"` first-touch provider)
- `src/lib/attribution.ts` — reader reconciled to contract keys
- `src/lib/orderTransform.ts` — native `_wc_order_attribution_*` forward via `buildAttributionMeta`; `_coach_ghl_*` + source_type bug removed
- `src/app/layout.tsx` — mount provider, delete dead coach-attribution `<Script>` + orphaned footer vars

**tests/:**
- `tests/lib/attributionCapture.test.ts` — **NEW** (25 tests incl. atomicity)
- `tests/api/place-order.test.ts` — +3 attribution forward tests

**docs (agent_docs/):**
- `templates/CONTRACT.md` — LOCKED-FINAL fold-in + FLAG 1
- `templates/EVIDENCE_LOG.md` — filled (Case A/B verbatim meta, assertions, findings)
- `CLEANUP_BACKLOG.md` — /types move + legacy-tracker retire items
- `RESPONSES/` — mirrors: plan, step1/2/3, finding1, phase2-readback
- `session_2026-07-29.md`, `RECOVERY.md`

**Commits/pushes:** NONE (standing rule — Tony's call).

---

## ⏸ REBOOT BOUNDARY — 2026-07-29 09:55

Tony rebooting machine. State at reboot:
- Working tree: same 5 modified + 2 untracked files (Coach's answer still not folded, `src/` still untouched). No commits.
- Task list: all completed (T2 Phase 0 tasks #11-#19 all done). Nothing in-progress.
- Awaiting: Tony's team's analysis of Coach's source_type answers → then fold-in to CONTRACT.md → then Phase 1 kickoff per `workflow/01_SOLUTION.md`.
- Post-reboot pickup: read `RECOVERY.md` → this file → `session_2026-07-08.md` (five locked decisions at `[09:35]`). No other state exists outside these files.
